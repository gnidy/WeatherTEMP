import { createTheme } from "@mui/material";

// Define the custom theme
const theme = createTheme({
  typography: {
    fontFamily: "Vazirmatn", // Use a valid font family
    fontSize: 14, // Default font   size
  },
});

export default theme;